# Given two integers a and b, you need to concatenate them so the output is ab and print it.

a = int(input())
b = int(input())

# Convert integers to strings, concatenate, then print
print(str(a) + str(b))
