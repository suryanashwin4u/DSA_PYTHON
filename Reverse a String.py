# You are given a string s, and your task is to reverse the string.
# Time Complexity: O(n)
# Auxiliary Space: O(1)

class Solution:
    def reverseString(self, s: str) -> str:
        return s[::-1]
